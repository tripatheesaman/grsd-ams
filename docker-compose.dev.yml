services:
  db:
    container_name: grsd-ams-db-dev
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    volumes:
      - ./media/db-dev:/var/lib/postgresql/data
    networks:
      - grsd_ams_network
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    container_name: grsd-ams-app-dev
    build: .
    env_file:
      - .env.development
    environment:
      NODE_ENV: production
      APP_ORIGIN: http://localhost:3000
      TEST_BUILD: ${TEST_BUILD:-0}
      PORT: 3000
      DATABASE_URL: postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@db:5432/${DATABASE_NAME}?schema=public
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./media/uploads:/app/media/uploads
      - ./media/processed:/app/media/processed
      - ./media/staging:/app/media/staging
      - ./logs:/app/logs
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - grsd_ams_network
    command: sh -c 'if [ "$TEST_BUILD" = "1" ]; then npm run build; fi && node .next/standalone/server.js'
    restart: unless-stopped

networks:
  grsd_ams_network:
    driver: bridge

