/*!
 * ASP.NET SignalR JavaScript Library 2.4.3
 * http://signalr.net/
 *
 * Copyright (c) .NET Foundation. All rights reserved.
 * Licensed under the Apache License, Version 2.0. See License.txt in the project root for license information.
 *
 */

/// <reference path="..\..\SignalR.Client.JS\Scripts\jquery-1.6.4.js" />
/// <reference path="jquery.signalR.js" />
(function ($, window, undefined) {
    /// <param name="$" type="jQuery" />
    "use strict";

    if (typeof ($.signalR) !== "function") {
        throw new Error("SignalR: SignalR is not loaded. Please ensure jquery.signalR-x.js is referenced before ~/signalr/js.");
    }

    var signalR = $.signalR;

    function makeProxyCallback(hub, callback) {
        return function () {
            // Call the client hub method
            callback.apply(hub, $.makeArray(arguments));
        };
    }

    function registerHubProxies(instance, shouldSubscribe) {
        var key, hub, memberKey, memberValue, subscriptionMethod;

        for (key in instance) {
            if (instance.hasOwnProperty(key)) {
                hub = instance[key];

                if (!(hub.hubName)) {
                    // Not a client hub
                    continue;
                }

                if (shouldSubscribe) {
                    // We want to subscribe to the hub events
                    subscriptionMethod = hub.on;
                } else {
                    // We want to unsubscribe from the hub events
                    subscriptionMethod = hub.off;
                }

                // Loop through all members on the hub and find client hub functions to subscribe/unsubscribe
                for (memberKey in hub.client) {
                    if (hub.client.hasOwnProperty(memberKey)) {
                        memberValue = hub.client[memberKey];

                        if (!$.isFunction(memberValue)) {
                            // Not a client hub function
                            continue;
                        }

                        // Use the actual user-provided callback as the "identity" value for the registration.
                        subscriptionMethod.call(hub, memberKey, makeProxyCallback(hub, memberValue), memberValue);
                    }
                }
            }
        }
    }

    $.hubConnection.prototype.createHubProxies = function () {
        var proxies = {};
        this.starting(function () {
            // Register the hub proxies as subscribed
            // (instance, shouldSubscribe)
            registerHubProxies(proxies, true);

            this._registerSubscribedHubs();
        }).disconnected(function () {
            // Unsubscribe all hub proxies when we "disconnect".  This is to ensure that we do not re-add functional call backs.
            // (instance, shouldSubscribe)
            registerHubProxies(proxies, false);
        });

        proxies['backgroundTaskHub'] = this.createHubProxy('backgroundTaskHub'); 
        proxies['backgroundTaskHub'].client = { };
        proxies['backgroundTaskHub'].server = {
            notifyTasksChanged: function () {
                return proxies['backgroundTaskHub'].invoke.apply(proxies['backgroundTaskHub'], $.merge(["NotifyTasksChanged"], $.makeArray(arguments)));
             }
        };

        proxies['bulkUpdateHub'] = this.createHubProxy('bulkUpdateHub'); 
        proxies['bulkUpdateHub'].client = { };
        proxies['bulkUpdateHub'].server = {
            ping: function () {
                return proxies['bulkUpdateHub'].invoke.apply(proxies['bulkUpdateHub'], $.merge(["Ping"], $.makeArray(arguments)));
             }
        };

        proxies['bulkUploadHub'] = this.createHubProxy('bulkUploadHub'); 
        proxies['bulkUploadHub'].client = { };
        proxies['bulkUploadHub'].server = {
            broadcast: function (method, data) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["Broadcast"], $.makeArray(arguments)));
             },

            getOnlineUsers: function () {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["GetOnlineUsers"], $.makeArray(arguments)));
             },

            getRealtimeStats: function () {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["GetRealtimeStats"], $.makeArray(arguments)));
             },

            getUserGroups: function () {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["GetUserGroups"], $.makeArray(arguments)));
             },

            joinBatchGroup: function (batchId) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["JoinBatchGroup"], $.makeArray(arguments)));
             },

            joinGroup: function (groupName) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["JoinGroup"], $.makeArray(arguments)));
             },

            leaveBatchGroup: function (batchId) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["LeaveBatchGroup"], $.makeArray(arguments)));
             },

            leaveGroup: function (groupName) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["LeaveGroup"], $.makeArray(arguments)));
             },

            sendToGroup: function (groupName, method, data) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["SendToGroup"], $.makeArray(arguments)));
             },

            sendToUser: function (userId, method, data) {
                return proxies['bulkUploadHub'].invoke.apply(proxies['bulkUploadHub'], $.merge(["SendToUser"], $.makeArray(arguments)));
             }
        };

        proxies['ChatHub'] = this.createHubProxy('ChatHub'); 
        proxies['ChatHub'].client = { };
        proxies['ChatHub'].server = {
            broadcast: function (method, data) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["Broadcast"], $.makeArray(arguments)));
             },

            getOnlineUsers: function () {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["GetOnlineUsers"], $.makeArray(arguments)));
             },

            getRealtimeStats: function () {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["GetRealtimeStats"], $.makeArray(arguments)));
             },

            getUserGroups: function () {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["GetUserGroups"], $.makeArray(arguments)));
             },

            joinGroup: function (groupName) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["JoinGroup"], $.makeArray(arguments)));
             },

            leaveGroup: function (groupName) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["LeaveGroup"], $.makeArray(arguments)));
             },

            send: function (name, message) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["Send"], $.makeArray(arguments)));
             },

            sendMessage: function (groupName, message) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["SendMessage"], $.makeArray(arguments)));
             },

            sendPrivateMessage: function (targetUserId, message) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["SendPrivateMessage"], $.makeArray(arguments)));
             },

            sendToGroup: function (groupName, method, data) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["SendToGroup"], $.makeArray(arguments)));
             },

            sendToUser: function (userId, method, data) {
                return proxies['ChatHub'].invoke.apply(proxies['ChatHub'], $.merge(["SendToUser"], $.makeArray(arguments)));
             }
        };

        proxies['NimbleRealTimeHub'] = this.createHubProxy('NimbleRealTimeHub'); 
        proxies['NimbleRealTimeHub'].client = { };
        proxies['NimbleRealTimeHub'].server = {
            broadcast: function (method, data) {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["Broadcast"], $.makeArray(arguments)));
             },

            getOnlineUsers: function () {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["GetOnlineUsers"], $.makeArray(arguments)));
             },

            getRealtimeStats: function () {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["GetRealtimeStats"], $.makeArray(arguments)));
             },

            getStats: function () {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["GetStats"], $.makeArray(arguments)));
             },

            getUserGroups: function () {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["GetUserGroups"], $.makeArray(arguments)));
             },

            joinGroup: function (groupName) {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["JoinGroup"], $.makeArray(arguments)));
             },

            leaveGroup: function (groupName) {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["LeaveGroup"], $.makeArray(arguments)));
             },

            refreshStats: function () {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["RefreshStats"], $.makeArray(arguments)));
             },

            sendToGroup: function (groupName, method, data) {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["SendToGroup"], $.makeArray(arguments)));
             },

            sendToUser: function (userId, method, data) {
                return proxies['NimbleRealTimeHub'].invoke.apply(proxies['NimbleRealTimeHub'], $.merge(["SendToUser"], $.makeArray(arguments)));
             }
        };

        proxies['overtimeSyncProgressHub'] = this.createHubProxy('overtimeSyncProgressHub'); 
        proxies['overtimeSyncProgressHub'].client = { };
        proxies['overtimeSyncProgressHub'].server = {
            broadcast: function (method, data) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["Broadcast"], $.makeArray(arguments)));
             },

            getOnlineUsers: function () {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["GetOnlineUsers"], $.makeArray(arguments)));
             },

            getRealtimeStats: function () {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["GetRealtimeStats"], $.makeArray(arguments)));
             },

            getUserGroups: function () {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["GetUserGroups"], $.makeArray(arguments)));
             },

            joinGroup: function (groupName) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["JoinGroup"], $.makeArray(arguments)));
             },

            joinSyncGroup: function (syncId) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["JoinSyncGroup"], $.makeArray(arguments)));
             },

            leaveGroup: function (groupName) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["LeaveGroup"], $.makeArray(arguments)));
             },

            leaveSyncGroup: function (syncId) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["LeaveSyncGroup"], $.makeArray(arguments)));
             },

            sendToGroup: function (groupName, method, data) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["SendToGroup"], $.makeArray(arguments)));
             },

            sendToUser: function (userId, method, data) {
                return proxies['overtimeSyncProgressHub'].invoke.apply(proxies['overtimeSyncProgressHub'], $.merge(["SendToUser"], $.makeArray(arguments)));
             }
        };

        return proxies;
    };

    signalR.hub = $.hubConnection("/signalr", { useDefaultPath: false });
    $.extend(signalR, signalR.hub.createHubProxies());

}(window.jQuery, window));
